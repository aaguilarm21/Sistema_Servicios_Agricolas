# BaseDatos

Aquí va el esquema y datos de base de datos.
- Archivo sqlite: `db.sqlite3`
- Para migrar y ver: `python manage.py migrate`, `python manage.py makemigrations`, `python manage.py dbshell`.
